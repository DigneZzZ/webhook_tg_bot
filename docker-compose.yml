services:
  webhook-bot:
    build: .
    container_name: webhook_tg_bot
    restart: unless-stopped
    ports:
      - "${WEBHOOK_PORT:-8080}:${WEBHOOK_PORT:-8080}"
    env_file:
      - .env
    volumes:
      - ./.env:/root/.env:ro
